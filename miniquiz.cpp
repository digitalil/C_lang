# include <stdio.h>

int main() {

	// 초기화시키면 쓰레기값이 담김

	/*
	double base;
	double height;

	base = 10;
	height = 4;

	// 0.5를 곱해서 실수화되었기 때문에 값이 0으로 나옴.
	// printf("정삼각형의 넓이는 %d * %d * 0.5 = %d", base, height, base * height * 0.5);
	printf("정삼각형의 넓이는 %.2lf * %.2lf * 0.5 = %.2lf", base, height, base * height * 0.5);
	*/
	
	/*
	// 정사각형 넓이
	double side;

	printf("정사각형의 넓이는 %.2lf * %.2lf = %.2lf \n", side, side, side*side);

	// 원의 둘레

	double r;
	double pi;

	r = 4;
	pi = 3.14;

	printf("원의 둘레는 2 * %.2lf * %.2lf = %.2lf\n", pi, r, 2 * pi * r);

	// 원의 넓이

	printf("원의 넓이는 %.2lf * %.2lf * %.2lf = %.2lf\n", pi, r, r, pi * r * r);
	*/


	

	return 0;
}